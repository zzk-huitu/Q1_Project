一：模型修改
  1.修改实体类中的表名		ok
  2.修改实体类中的列表		ok	
  3.修改实体类中的映射数据库列的属性 	ok
  4.检查使用到备用字段的表，使用明确的字段代替（最终取消实体类中的备用字段）
  5.修改各个子查询语句代码，把字段和表名都该正确
  6.采用新的id生成方式，并成功运用
  7.分配模型类到各个package包下
  8.修改类名，使用重构的方式修改
  9.将俊哥目前UP中的相关表进行迁移（正式迁移之后再进行迁移，边迁移边设定模型）  
  N.检查各个表中是否存在无用字段或需要增加字段

二：服务层修改
  1.将服务层中涉及到模型类的地方，改为新的字段（ok）
  2.检查使用到hql、sql的地方，修改字段和表名为新信息
 

三：web层修改
  1.将服务层中涉及到模型类的地方，改为新的字段（ok）
  2.检查使用到hql、sql的地方，修改字段和表名为新信息

四：修改原始代码，使程序能够正常的像旧版本一样运行
  1.针对新的模型类，分别检查dao、service、web层的相影响的部分
  2.将所有用到creatUser的地方，都改为用户的id
  3.将服务层中，使用到字段名字符串的地方进行检测（因为不会自动报错）
  4.检查使用到备用字段的表，使用明确的字段代替，最终删除实体类中的备用字段。（增加之后，更新数据字典）
  5.
....